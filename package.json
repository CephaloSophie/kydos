{
  "name": "belote-contree",
  "version": "11.13.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "server",
    "web",
    "mobile"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c yellow,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --workspace belote-server run dev",
    "dev:web": "npm --workspace belote-web run dev",
    "build:web": "npm --workspace belote-web run build",
    "typecheck": "npm --workspace belote-core run typecheck && npm --workspace belote-server run typecheck && npm --workspace belote-web run typecheck",
    "mongo": "docker compose up -d mongo",
    "test": "npm --workspace belote-core run test && npm --workspace belote-web run test",
    "dev:mobile": "npm --workspace belote-mobile run dev",
    "typecheck:all": "npm --workspace belote-core run typecheck && npm --workspace @kydos/table-pixi run typecheck && npm --workspace belote-server run typecheck && npm --workspace belote-web run typecheck && npm --workspace belote-mobile run typecheck",
    "test:all": "npm --workspace belote-core run test && npm --workspace @kydos/table-pixi run test && npm --workspace belote-server run test && npm --workspace belote-web run test && npm --workspace belote-mobile run test",
    "build:all": "npm --workspace belote-web run build && npm --workspace belote-web run build:lib && npm --workspace belote-mobile run build",
    "tnr": "node scripts/tnr.mjs",
    "seed": "npm --workspace belote-server run seed",
    "tnr:server": "node scripts/tnr-server.mjs",
    "coverage": "node scripts/coverage.mjs",
    "e2e:web": "npm --workspace belote-web run e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@vitest/coverage-v8": "^2.1.9",
    "concurrently": "^8.2.2"
  }
}
