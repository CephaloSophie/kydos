# Bord — configuration serveur
# Copier en .env pour lancer (les défauts ci-dessous sont ceux du dev local).

PORT=4100
MONGO_URI=mongodb://root:toor@127.0.0.1:27017/bordjira?authSource=admin
JWT_SECRET=change-me-in-prod-32-chars-min
CORS_ORIGIN=http://localhost:4200,http://127.0.0.1:4200
