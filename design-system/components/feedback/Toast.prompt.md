Notification éphémère. Web : pile en haut à droite. Mobile : pile au-dessus de la tab bar (`.m-toast-host` + `m-anim-toast-in`).

```jsx
<Toast tone="success" title="Pli remporté" message="Sud ramasse (R♥)" onClose={dismiss} />
<Toast tone="spark" title="Iznogoud a contré" />
```
