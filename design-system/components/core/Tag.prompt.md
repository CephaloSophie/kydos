Étiquette filtre ou attribut (filtres du lobby, tags de robot). Sélectionnable et/ou retirable.

```jsx
<Tag selected onClick={toggle}>Tables privées</Tag>
<Tag onRemove={() => drop('agressif')}>agressif</Tag>
```
