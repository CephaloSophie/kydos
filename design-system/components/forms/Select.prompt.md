Liste déroulante sombre (convention d'enchères du robot, sens de jeu, manches par défaut). Listbox accessible au clavier.

```jsx
<Select label="Convention" value={conv} onChange={setConv}
  options={[{value:'classique',label:'Classique'},{value:'soft',label:'Soft'}]} />
```
